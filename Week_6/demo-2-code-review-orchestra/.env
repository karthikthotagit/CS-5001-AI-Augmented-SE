# Ollama server (default: local)
OLLAMA_HOST=http://localhost:11434

# Model to use — must be pulled first: ollama pull llama3.2:3b
AGENT_MODEL=gpt-oss:20b-cloud

# Max tokens to generate per LLM call
# MAX_TOKENS=2048
